import styled from 'styled-components/native';

export const Container = styled.View`
  width: 100%;
  height: ${({ theme }) => theme.SPACINGS.XL}px;
  justify-content: center;
  align-items: center;
`;
